`start_server()` now accepts an optional `runner` argument (default: `asyncio.run`) to allow using custom coroutine runner implementations such as `uvloop.run`, for forward compatibility with Python 3.16 and later

Add Windows support for `fork` and `server` modules with `WindowsChildProcess` class, cross-platform signal handling, and optional `winloop` integration for improved event loop performance on Windows
